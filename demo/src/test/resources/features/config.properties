# Environment
base.url=https://example.com/login
browser=chrome
implicit.wait=10
page.load.timeout=20

# Test Credentials (for demo only — don’t store real passwords here)
username=testuser
password=testpass

# Execution
headless=false
